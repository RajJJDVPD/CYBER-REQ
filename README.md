# CYBER-REQ 🚀

A GUI-based tool built in Python (Tkinter) to send N HTTP requests to a target URL.

## Features
- GUI-based interface
- Optional delay control
- Logs status of each request

## Setup
1. Install Python 3
2. Install dependencies:
